# API Key Storage

Saved API keys are written here as JSON files, one file per provider.

Example:

```json
{
  "provider": "OpenAI GPT",
  "api_key": "your-api-key"
}
```

Do not share files from this folder. The app ignores invalid or missing JSON files.
